# Frontend (Vite) — public only. Never put server secrets in VITE_* vars.
VITE_API_BASE_URL=http://localhost:8787
VITE_DATA_MODE=local

# Optional legacy Gemini browser key (remove in Wave 11; server proxy then).
# GEMINI_API_KEY=
