# Development defaults — do not use these credentials in production.
PORT=8787
DATABASE_URL=postgres://bcba:bcba_dev_only@localhost:5432/bcba_workspace
# Comma-separated allowlist (never * with credentials)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
SESSION_SECRET=dev-only-session-secret-change-me
DEV_SEED_PASSWORD=DevOnlyPass123!
NODE_ENV=development
